fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1.4
python-multipart>=0.0.9
apscheduler>=3.10
qrcode[pil]>=7.4
Pillow>=10.2
openpyxl>=3.1

# LINE integration is optional: the app falls back to plain HTTPS calls,
# but the SDK enables Flex messages and the rich menu.
line-bot-sdk>=3.7

# dev / test
pytest>=8.0
pytest-cov>=5.0
httpx>=0.26
ruff>=0.5
